// YOU CAN ALSO MANAGE STYLES USING CUSTOM COMPONENTS (IE, BODYTEXT and TITLE COMPONENTS)
import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    bodyText: {
        fontFamily: 'open-sans',
    }
})